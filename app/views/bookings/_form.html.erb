<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.collection_select :user_id, User.order(:name), :id, :name, prompt: '' %>
  </div>
  <div class="field">
    <%= f.label :item_id %>
    <%= f.collection_select :item_id, Item.order(:name), :id, :name, prompt: '' %>
  </div>
  <div class="field">
    <%= f.label :started_at %><br>
    <%= f.date_select :started_at %>
  </div>
  <div class="field">
    <%= f.label :ended_on %><br>
    <%= f.date_select :ended_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cars_id %><br>
    <%= f.number_field :cars_id %>
  </div>
  <div class="field">
    <%= f.label :start_dates %><br>
    <%= f.date_select :start_dates %>
  </div>
  <div class="field">
    <%= f.label :end_dates %><br>
    <%= f.date_select :end_dates %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
